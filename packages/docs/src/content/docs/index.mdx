---
title: Rapid Form — The 2KB React Form Validation Hook
description: rapid-form is a tiny, zero-dependency React hook for native HTML form validation. No schema, no register(), no re-renders on every keystroke. Just attach a ref and go.
template: splash
hero:
  tagline: The 2KB React form hook that just works. No schema. No register(). No re-renders.
  image:
    file: ../../assets/houston.webp
  actions:
    - text: Getting Started
      link: /rapid-form/getting-started/installation/
      icon: right-arrow
      variant: primary
    - text: Why Rapid Form?
      link: /rapid-form/why-rapid-form/
      icon: information
      variant: secondary
    - text: GitHub Repo
      link: https://github.com/Randagio13/rapid-form
      icon: external
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div style={{ display: 'flex', gap: '10px', justifyContent: 'center', alignItems: 'center', flexWrap: 'wrap', marginBottom: '2rem' }}>
  <a href="https://www.npmjs.com/package/rapid-form" target="_blank" rel="noopener">
    <img src="https://img.shields.io/npm/v/rapid-form?style=flat-square&color=7C3AED" alt="npm version" />
  </a>
  <a href="https://www.npmjs.com/package/rapid-form" target="_blank" rel="noopener">
    <img src="https://img.shields.io/npm/dm/rapid-form?style=flat-square&color=7C3AED" alt="npm downloads" />
  </a>
  <a href="https://github.com/Randagio13/rapid-form/stargazers" target="_blank" rel="noopener">
    <img src="https://img.shields.io/github/stars/Randagio13/rapid-form?style=flat-square&color=7C3AED" alt="GitHub stars" />
  </a>
  <a href="https://github.com/Randagio13/rapid-form/blob/main/LICENSE" target="_blank" rel="noopener">
    <img src="https://img.shields.io/npm/l/rapid-form?style=flat-square&color=7C3AED" alt="license" />
  </a>
</div>

## See it in action

```tsx
import { useRapidForm } from 'rapid-form';

export function ContactForm() {
  const { refValidation, errors, values } = useRapidForm();

  return (
    <form ref={(ref) => refValidation(ref)}>
      <input name="email" type="email" required placeholder="you@example.com" />
      {errors.email?.isInvalid && (
        <span>{errors.email.message}</span>
      )}

      <input name="name" required placeholder="Your name" />
      {errors.name?.isInvalid && <span>{errors.name.message}</span>}

      <button type="submit">Send</button>
    </form>
  );
}
```

That's the whole thing. No schema. No `register()`. No `Controller`. Just native HTML.

## Why Rapid Form?

<CardGrid stagger>
  <Card title="~2KB" icon="rocket">
    Zero runtime dependencies. Ships in under 2KB minified and gzipped. react-hook-form is ~25KB. Your users will notice.
  </Card>
  <Card title="Native HTML" icon="document">
    Any element with `name` + `required` is automatically validated. No `register()`. No `Controller`. No wrappers.
  </Card>
  <Card title="No re-renders" icon="approve-check-circle">
    Validation is event-driven, not controlled. Your form doesn't re-render on every keystroke. It stays fast.
  </Card>
  <Card title="Any field type" icon="list-format">
    `input`, `select`, `textarea`, `checkbox` — all supported out of the box. If the browser knows it, Rapid Form handles it.
  </Card>
</CardGrid>
